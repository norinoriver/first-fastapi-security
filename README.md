# FastAPI Initializer at VSCode

1. create .venv
```
pwd 
$ `your-device-path`/fastapi-init-vscode
mkdir .venv
ls -a | grep .venv
$ .venv

```
2. let's install module by poetry 
```
poetry install
```